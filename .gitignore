# Secrets — never commit real keys
.env

# Local corpus (default lives in ~/.research-mcp/, but guard the repo path too)
*.db
*.db-wal
*.db-shm
corpus.db

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
